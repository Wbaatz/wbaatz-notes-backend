services:
  - type: web
    name: wbaatz-notes-backend
    env: node
    plan: free
    buildCommand: cd api-server && npm run build
    startCommand: cd api-server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
